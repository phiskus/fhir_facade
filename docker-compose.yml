services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: fhir_facade
      POSTGRES_USER: fhir_app
      POSTGRES_PASSWORD: localdev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: